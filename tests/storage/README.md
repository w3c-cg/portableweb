# Storage Model Tests

Test cases for **Goal 4: Per-bundle isolated storage model**.

This directory will contain tests covering:

- Storage isolation between bundles with different `id` values
- Storage persistence across viewer sessions for the same bundle `id`
- Storage quota behavior
- `exportData()` / `importData()` portability hooks
- Snapshot bundle behavior

*No test cases yet — this goal is pending spec completion.*
