# Manifest Schema Tests

Test cases for **Goal 2: Manifest schema and capability declaration**.

This directory will contain tests covering:

- Required manifest fields (`spec_version`, `id`, `version`, `title`, `entry`)
- Field type and value validation
- Permissions / capability declaration schema
- Default-deny enforcement

*No test cases yet — this goal is pending spec completion.*
